# Pipelines

This directory provides the canonical pipeline entrypoint for `okoljski_raziskovalni_model`.

The current implementation still delegates to the established scripts in `../scripts/` for backward compatibility, but the repository should be presented as having a dedicated pipeline layer.

Canonical runner:

- `run_okoljski_raziskovalni_model.py`
