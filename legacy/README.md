# Legacy pre-v3 implementation

This directory contains the archived pre-v3 implementation. It is retained only for historical reference and is not part of the active pipeline.

`model_v3` must not import from this directory. Files under `legacy/` should not be modified unless explicitly requested.
