# Frontend

`frontend/` vsebuje uporabniški vmesnik spletne aplikacije.

- `public/`: statične datoteke
- `src/components/`: manjši gradniki vmesnika
- `src/pages/`: zasloni aplikacije
- `src/styles/`: stile in grafično podobo
